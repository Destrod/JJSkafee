# ADR-0012: Selección del motor de base de datos relacional

## Estado
Aceptado

## Criterios de evaluación
**Resumen:** Se buscaba determinar qué motor de base de datos relacional soporta mejor los requisitos ya definidos en otros ADR: aislamiento multi-tenant robusto (ADR-0001), consultas agregadas eficientes con volumen creciente (ADR-0004), y compatibilidad con el ecosistema de proveedores externos ya seleccionados.

**Detalles:**
- Soporte nativo de Row Level Security (RLS), validado como mecanismo de aislamiento multi-tenant en SPIKE-03.
- Capacidad de indexación y rendimiento en consultas agregadas con volumen de 10.000+ movimientos por cliente.
- Costo de hosting administrado.
- Facilidad de integración con Node.js (o el lenguaje de backend que finalmente se elija).
- Madurez y soporte de la comunidad.

## Candidatos a considerar
**Resumen:** Se identificaron los motores relacionales más usados en productos SaaS similares, incluyendo la opción de un proveedor administrado que integra base de datos y otras capacidades.

**Candidatos:**
- MySQL (autoadministrado o mediante un proveedor de hosting).
- PostgreSQL (autoadministrado o mediante un proveedor de hosting).
- Supabase (PostgreSQL administrado, con autenticación y almacenamiento integrados).

## Investigación y análisis de cada candidato

### Cumple/no cumple con los criterios y por qué
- **MySQL:** Cumple parcialmente — no ofrece Row Level Security de forma nativa como funcionalidad madura y de uso extendido; el aislamiento tendría que resolverse enteramente a nivel de aplicación, sin la segunda capa de protección validada en SPIKE-03.
- **PostgreSQL:** Cumple — soporta Row Level Security de forma nativa, exactamente como se validó en SPIKE-03, además de buen soporte de indexación y tipos de datos avanzados (JSON, arrays) útiles para funcionalidades futuras.
- **Supabase:** Cumple — es PostgreSQL por debajo, por lo que hereda las mismas capacidades de RLS, con la ventaja adicional de un panel administrado y posible superposición con el proveedor de autenticación.

### Análisis de costos

**MySQL:**
- Licencias: gratuito (código abierto); costo de hosting administrado similar al de PostgreSQL en la mayoría de proveedores.
- Capacitación: mínima, ampliamente conocido.
- Operación: el aislamiento multi-tenant requeriría desarrollo y mantenimiento adicional en el código de aplicación, sin el respaldo de RLS.
- Medición: mayor esfuerzo de desarrollo y mayor riesgo residual de seguridad al no contar con la segunda capa de aislamiento ya validada.

**PostgreSQL:**
- Licencias: gratuito (código abierto).
- Capacitación: mínima-media, requiere familiaridad con la sintaxis de políticas de RLS (ya validada en SPIKE-03).
- Operación: costo de hosting administrado (ej. Neon, Render, Railway, RDS), estimado entre $150.000 y $400.000 COP/mes para el volumen proyectado.
- Medición: cumple directamente con el diseño ya validado en SPIKE-03, sin retrabajo.

**Supabase:**
- Licencias: nivel gratuito disponible para volúmenes bajos; plan Pro desde aproximadamente $25 USD/mes.
- Capacitación: mínima, incluye panel de administración visual para las políticas de RLS.
- Operación: soporte y mantenimiento de infraestructura a cargo de Supabase.
- Medición: costo competitivo, con la ventaja de que autenticación (alternativa a Firebase, ver ADR-0001) y base de datos podrían administrarse desde el mismo proveedor si se decidiera cambiar esa decisión más adelante.

### Análisis FODA

**MySQL:**
- Fortalezas: muy extendido, amplia documentación y comunidad.
- Debilidades: sin Row Level Security nativo maduro, obliga a reconstruir el mecanismo de aislamiento validado en SPIKE-03.
- Oportunidades: ninguna relevante frente a PostgreSQL para este caso de uso específico.
- Amenazas: mayor riesgo de fuga de datos entre cafeterías al depender únicamente del filtro de aplicación, sin una segunda capa de protección a nivel de base de datos.

**PostgreSQL:**
- Fortalezas: soporte nativo de RLS ya validado, buen rendimiento en consultas agregadas, tipos de datos flexibles para necesidades futuras (ej. datos semiestructurados de recomendaciones de IA).
- Debilidades: la sintaxis de RLS requiere una curva de aprendizaje inicial para el equipo.
- Oportunidades: es el motor recomendado por Supabase, dejando abierta la puerta a simplificar el stack en el futuro sin migrar de motor.
- Amenazas: ninguna significativa identificada.

**Supabase:**
- Fortalezas: administración simplificada, incluye funciones adicionales (almacenamiento de archivos, funciones en tiempo real) que podrían ser útiles a futuro.
- Debilidades: atarse a un proveedor único para base de datos (y potencialmente autenticación) aumenta la dependencia de un solo tercero.
- Oportunidades: podría simplificar la arquitectura si se decide migrar la autenticación de Firebase a Supabase Auth más adelante.
- Amenazas: menor control fino sobre la configuración del servidor de base de datos comparado con una instancia de PostgreSQL autoadministrada o contratada directamente con un proveedor de hosting genérico.

### Opiniones y comentarios internos
**Resumen:** El equipo prioriza mantener consistencia con el diseño de aislamiento multi-tenant ya validado técnicamente en SPIKE-03, el cual se construyó y probó específicamente sobre PostgreSQL.

### Opiniones y comentarios externos
**Resumen:** No se realizó una consulta externa formal adicional; la decisión se apoya directamente en el resultado ya obtenido en SPIKE-03.

## Recomendación
**Resumen:** Usar PostgreSQL como motor de base de datos relacional, ya sea autoadministrado en un proveedor de hosting genérico o mediante Supabase, priorizando en cualquier caso el soporte nativo de Row Level Security validado en SPIKE-03.

**Detalles:** Se recomienda iniciar con un proveedor de hosting de PostgreSQL simple (ej. Render, Railway o Neon) por costo y simplicidad, dejando la migración a Supabase como opción a evaluar si en el futuro se decide consolidar autenticación y base de datos en un mismo proveedor.
